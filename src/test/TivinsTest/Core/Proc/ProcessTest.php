<?php

namespace TivinsTest\Core\Proc;

use Tivins\Core\Proc\Process;

class ProcessTest
{
    public function testEnv()
    {
        $proc = new Process();
        // $result = $proc->run(new Command('php'), int $asyncFreq = 0, ?string $stdin = null): ProcInfo
    }
}